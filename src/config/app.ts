export const appName = 'Synapse'
export const appTitle = 'Synapse IIoT'

/** Peran platform ini — muncul di footer setiap halaman. */
export const platformRole = 'Industrial IoT Gateway & Mini-SCADA'

/** Ukuran halaman baku untuk seluruh daftar terpaginasi. */
export const PAGE_SIZE = 10

/** Titik data realtime yang ditahan per perangkat (FIFO) sebelum digambar. */
export const REALTIME_WINDOW = 30
